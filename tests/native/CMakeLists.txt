# Phase 1 gate tests — thin wrappers around upstream C++ test code.
# These run under ctest and must all pass before Phase 1 is signed off.
# See also: individual test targets in nucleus/io and realigner subdirectories.
